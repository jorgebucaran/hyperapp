{
  "name": "hyperapp",
  "description": "The tiny framework for building hypertext applications",
  "version": "2.0.4",
  "type": "module",
  "main": "src/index.js",
  "unpkg": "dist/index.js",
  "license": "MIT",
  "repository": "jorgebucaran/hyperapp",
  "homepage": "https://hyperapp.dev",
  "files": [
    "src",
    "dist"
  ],
  "author": "Jorge Bucaran",
  "keywords": [
    "hyperapp",
    "frontend",
    "framework",
    "virtual dom",
    "vdom",
    "ui"
  ],
  "scripts": {
    "test": "echo 0",
    "docs": "cd docs && npm run build",
    "build": "export dir=${pkg:+lib/$pkg/} pkg=$npm_package_name$pkg; npm run bundle && npm run minify && npm run unpkg",
    "unpkg": "terser ${dir}$npm_package_main -o ${dir}$npm_package_unpkg  -mc --source-map includeSources,url=$npm_package_main.map --module",
    "minify": "terser ${dir}dist/$pkg.js -o ${dir}dist/$pkg.js -mc --source-map includeSources,url=$pkg.js.map",
    "bundle": "rollup -i ${dir}$npm_package_main -o ${dir}dist/$pkg.js --no-strict -mf iife -n $pkg",
    "create": "npm run build && git commit -am $msg && git tag -s $msg -m $msg && git push && git push --tags",
    "release": "env msg=$(npm run -s message) npm run create && cd ./${pkg:+lib/$pkg} && npm publish --access public",
    "message": "echo ${pkg:+@$npm_package_name/$pkg@}$(node -p \"require('./${pkg:+lib/$pkg/}package').version\")"
  },
  "devDependencies": {
    "rollup": "*",
    "terser": "*"
  }
}
